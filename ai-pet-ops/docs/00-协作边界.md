# 协作边界

`ai-pet-ops` 是跨仓运维项目，不拥有业务领域数据或接口定义。

- 业务接口契约仍以 `ai-pet-backend/docs/06-HTTP-API规范.md` 为准。
- xiaozhi-server 的集成契约仍以 `xiaozhi-server/docs/05-与业务后端集成接口.md` 为准。
- 本仓只探测公开或 loopback 健康入口，不解释用户数据、对话、记忆或人设内容。
- 任何新增端口、反向代理、服务器权限、告警接收方均需先写入本仓部署文档并经人工确认。
- 不允许采集器自动重启、删除或更新现有容器。
