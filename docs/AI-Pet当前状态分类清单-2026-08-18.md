# AI Pet 当前状态分类清单

> 快照日期：2026-08-18
> 工作区：`D:\Home_Work`
> 用途：按“已完成、待测试、已验收、未完成、有 Bug、未开始”统一记录当前项目状态。
> 注意：本文是状态快照，不替代接口契约、协作看板或各仓进度真源。

## 1. 信息来源与快照边界

本次状态依据以下来源交叉核对：

- `work_dashboard/AI-Pet项目全景与进度.md`
- `work_dashboard/AI-Pet协作看板.md`
- `work_dashboard/AI-Pet固件联调看板.md`
- 各仓 `AGENTS.md`、`docs/00*`、Git 状态、近期提交、源码路由和构建配置
- `ESP32_XIAOZHI/xiaozhi-esp32/AI_PET_PROGRESS_zh.md`
- `prototype/e2e-checklist.html` 及其他产品原型
- `hardware/` 中的 ESP32-S3 原理图审核材料

### 1.1 当前工作区状态

| 项目 | 当前检出状态 | 备注 |
|---|---|---|
| 总仓 `AI_pet` | `main`，正在收口本快照 | 各子仓均已推送；待提交本文与最终 submodule 指针 |
| `work_dashboard` | `main=3451daf`，干净 | 与 `origin/main` 对齐；三份看板已同步最终部署与联调状态 |
| `xiaozhi-server` | `main=99aa353`，工作树干净 | 与 `origin/main` 对齐；b13 已将无语音会话窗口延长至 300 秒并部署 |
| `ai-pet-backend` | `main=4b2fc62`，干净 | 与 `origin/main` 对齐并已部署；迁移 0010，趣味测试/简略星盘上线 |
| `ai-pet-admin` | `main=efdd8cd`，干净 | 与 `origin/main` 对齐；自动构建/提交/部署/推送约定已入库 |
| `ai-pet-app` | `main=35f297c`（功能 `ffd04ae`），干净 | 与 `origin/main` 对齐并已部署；公网资产 `index-BhrOz7WB.js` |
| `prototype` | detached `d640f2b`，干净 | 第五次原型校准版本 |
| `xiaozhi-esp32` | `main=44b1c4d`，干净 | 与 `origin/main` 对齐；E11 主动播报架构设计稿已提交，尚无实现代码 |

### 1.2 状态口径

| 状态 | 判定口径 |
|---|---|
| 已完成 | 代码、配置或文档已经实现；不自动等于真机或生产验收通过 |
| 待测试 | 实现已存在，但缺构建、部署、真机、真实账号或故障场景证据 |
| 已验收 | 已有明确的测试、容器、线上、浏览器或真机证据 |
| 未完成 | 已开工但仅完成部分，或因上游依赖/产品决策阻塞 |
| 有 Bug | 已确认缺陷、安全问题、可复现性问题或状态冲突；注明是否已有修复 |
| 未开始 | 尚无对应实现，仅有设计、排期或需求 |

## 2. 总体结论

AI Pet 已具备可内测的主链路：设备 OTA/激活/语音、小智实时编排、业务后端、管理台和用户端均已上线。当前项目不是“功能骨架阶段”，而是“核心能力已交付、等待真机端到端收口和正式发布基础设施”的阶段。

当前最高优先级不是继续扩展功能，而是：

1. 完成 X1 真机 E2E 验收并取得可追溯日志证据。
2. 对 App `35f297c` 做真实账号/数据验收，覆盖档案、问卷、导出、画像、E10 与测试/星盘。
3. 处理已提交天气 API Key、固件生成资产缺失和 OTA 空间风险。
4. 完成域名、ICP、HTTPS/WSS 和正式运维监控。
5. 解决固件 OTA 空间后，再进入第二只眼及行为外设开发。

## 3. 已验收

### 3.1 设备与实时语音

- 自建 OTA 地址可达，真机已取得激活码并完成激活绑定。
- 设备 WebSocket 握手和首轮语音链路已真机通过：唤醒、ASR、LLM、TTS、眼睛情绪联动可工作。
- 眼睛 MCP 基础链路已真机验证：`look`、`blink`、`close`、`open`、`set_emotion` 可被工具发现和路由；历史证据覆盖其中主要动作。
- 服务端 8000/8002/8003 和 4 个基础容器已有公网/容器存活验证。

### 3.2 业务后端

- web-api、memory-mcp、agent-worker、PostgreSQL、Redis 已部署运行。
- 注册、登录、JWT、设备资产、`binding_id` 认领、设备列表/详情/改名/解绑已上线。
- 人设 GET/PUT、12 星座、16 型 MBTI、KB v2、dossier 六字段已部署。
- 对话历史脱敏查询、带时间窗删除和审计记录已上线。
- 用户端及管理端记忆 CRUD/审核、分析读取、外设快照读取已上线。
- KB 草稿、版本递增、发布和反馈审核已上线。
- `daily_summary` Worker 已用真实会话产出摘要；2026-08-16 重跑 8 个任务均完成，并清理重复分析记录。
- Memory MCP HTTP `/mcp` 已完成容器级鉴权、初始化、`tools/list` 和 `memory.search` 验收。

### 3.3 管理台与用户端

- 管理台 `:8080` 已完成登录、会话保持、路由守卫和同源反代验收。
- 管理台已实现设备资产、绑定码轮换、人设、dossier、历史、记忆审核、分析、外设和 KB 运营。
- 用户端 `:8081` 已完成注册登录、绑定码认领、多设备切换、人设、历史、记忆、日运和外设状态展示。
- 两个前端均有生产构建通过和 ECS HTTP 探活记录。

### 3.4 部署与访问

- backend 仅通过本机/容器网络提供 8010 或 `web-api:8000`，未直接暴露业务 API 公网端口。
- mini 主机专用 Ed25519 SSH 密钥已生成、追加到服务器并实际登录成功；原有其他机器公钥未覆盖。
- Memory MCP 未映射公网端口，内部 Token 错误时返回 401。
- `business_api`/C5 的生产基址已从会被 UFW 丢弃的 `host.docker.internal:8010` 修正为 Docker 内网 `web-api:8000`。

## 4. 已完成

### 4.1 xiaozhi-server 已实现能力

- persona_pack 首次拉取、300 秒刷新、缓存和 onboarding 降级。
- C5 动态上下文注入最终 Prompt。
- MiniMax `<think>` 跨 chunk 过滤、关闭思考参数和 direct-answer 兜底。
- chat events、session end、devices/seen、peripheral events 五类业务旁路。
- BIZ 统一旁路日志，不记录对话正文、Token 或完整 Prompt。
- Memory MCP 三工具白名单、1.2 秒超时和失败降级。
- 远端 b11 已实现智控台已绑定设备自动导入 backend：启动扫描并每 30 秒复查。
- 双机对聊房间的职责已确定归 xiaozhi-server，backend 不承担实时会话桥。

### 4.2 backend 已实现能力

- FastAPI monorepo：web-api、memory-mcp、agent-worker、persona-compiler 和共享数据层。
- 14 张业务模型表、7 个迁移、PG `SKIP LOCKED` 异步任务队列。
- 当前测试源码包含 66 个测试函数，覆盖 auth、devices、internal、persona、messages、redaction、worker 和 smoke 等领域。
- persona compiler 已注入明确身份行，修复宠物否认自身星座的问题。
- Worker 已处理 LLM 401/403/429/5xx/网络异常延迟重试，并剥离思考标签后解析 JSON。

### 4.3 admin/app/prototype/ops 已完成能力

- admin dossier 六字段编辑器已经在代码中完成；旧看板 D5“待开发”状态已经过时。
- prototype 已交付总览、真机验收墙、人设初始化和“我的星仔”原型。
- ai-pet-ops 已完成零第三方依赖的只读采集器骨架、示例配置和 systemd 模板。
- 总仓已用 submodule 管理 backend、xiaozhi、admin、app、dashboard、prototype 和固件仓。

### 4.4 固件已完成能力

- ESP32-P4 AI Pet 自研板型、I2C 音频、GC9A01 单眼和 BOOT 键已实现。
- 眼睛支持情绪、视线、眨眼、闭眼和自动 idle 开关。
- 5 个眼睛 MCP 工具已注册。
- 板型 `config.json` 已指定 32MB Flash 和 `partitions/v2/32m.csv`。
- 程序化眼睛资产生成脚本和 RGB565 转换工具已存在。

## 5. 待测试

### 5.1 X1 真机端到端验收

以下三项已由用户真机确认通过：`<think>` 不播报、首轮回复体现 C5、设备正确承认星座与 MBTI。以下能力仍缺完整真机证据：
- persona_pack 修改后在下一次会话或刷新周期生效。
- user/assistant 两类 chat events 实际落库。
- devices/seen 自动建档及后台资产可见。
- session end 断开后正确结束会话并入队任务。
- 眼睛 MCP 后 peripheral 快照实际落库。
- 真机对话至少成功调用一次 `memory.search`。
- BIZ 日志能够给出上述操作的 device_uid、session_id、耗时和 outcome 证据。

### 5.2 服务端体验回归

- S1：关闭开场唤醒回复。
- S2：说“休息/睡觉/晚安”并成功闭眼后结束会话。
- S3：b13 已将无语音窗口调整为 300 秒，待真机验证五分钟内续聊与超时断连。
- S4：24kHz 下行音频回归。
- S5：五个眼睛工具完整真机回归。
- 语音打断/TTS abort 与 turn-id 作废链。
- 断网、backend 5xx、Memory MCP 超时和空 persona 等降级演练。

### 5.3 b12 真机回归

xiaozhi-server 可靠性修复已经提交、合并、构建并部署，容器级定向验收通过。现在只需并入 X1 真机回归，确认严格 FIFO、批量眼睛快照/休息断开及带 persona 提示的 `memory.search` 在真实设备链路中生效。
### 5.4 前端和固件待测试

- App 日运页需用真实用户账号验证 8 条新 `daily_summary`。
- App 历史、记忆、外设页面仍需真实设备连续数据验收。
- PWA 安装必须在域名和 HTTPS 完成后测试。
- 设备端 AEC + realtime 打断已有编译记录，但仍待烧录和真机验收。
- 固件第二只眼、共享 SPI 和双屏同步尚无验证条件。
- S3 原理图只有评审结论，投板前 P0 关闭后仍需 EVT bring-up。

## 6. 有 Bug 或明确风险

### 6.1 安全问题

| 问题 | 当前状态 | 处理建议 |
|---|---|---|
| `xiaozhi-server/.../config.yaml` 中存在高概率真实的和风天气 API Key | 未处理 | 立即轮换；从当前版本和 Git 历史清除；部署密钥改放私有配置 |
| App/Admin 仍通过 HTTP 内测地址访问 | 已知发布阻塞 | 完成域名、ICP、HTTPS/WSS 后再开放正式用户 |
| 公网 SSH | 已使用密钥登录 | 安全组 22 仅允许可信公网 IP；不要删除其他机器公钥 |

### 6.2 已确认功能缺陷

| 问题 | 状态 |
|---|---|
| 业务旁路旧重试通过 Timer 重新入队，可能破坏 FIFO 顺序 | b12 已修复并通过容器验收，待真机回归 |
| 批量工具调用没有执行单工具路径的外设快照和休息断开副作用 | b12 已修复并通过容器验收，待真机回归 |
| 2026-08-17 旁路走 `host.docker.internal:8010` 被 UFW 丢弃，消息事件超时丢失且不可回放 | 地址已修为 `web-api:8000`；需防止配置回退并做真机回归 |
| WakeNet 误唤醒率偏高 | 未修复，需固件侧调阈值/麦增益 |
| `blink_profile` 在 persona_pack 中存在，但固件没有运行时眨眼参数 MCP | 能力边界已澄清；当前不得宣称字段生效 |

### 6.3 构建与可复现性问题

| 问题 | 影响 |
|---|---|
| 固件 CMake 和 `pet_eye_display.cc` 引用 9 个状态帧 `.bin`，但这些文件被 `*.bin` 忽略且干净克隆中不存在 | 新机器可能在链接阶段失败；应纳入 CMake 生成或强制提交确定性资产 |
| 固件应用镜像约占 OTA 槽 92.53% | 第二只眼、灯带、舵机、视觉和新行为代码无法安全继续堆叠 |
| `main/pet/` 类型层曾在旧环境作为未提交设计存在，当前克隆中不存在 | 平台矩阵和 K230 类型层无法从仓库复现 |

### 6.4 文档和协作状态问题

| 问题 | 当前事实 |
|---|---|
| 根目录旧 `AI-Pet协作看板.md` 使用 `/internal/*` | 正式契约已统一为 `/api/internal/*`，旧文件会误导新会话 |
| admin `docs/api-openapi.json` 陈旧 | 仍描述 MAC 直绑、整数 session_id，且缺少大量已上线接口 |
| backend `memory_mcp/server.py` 顶部注释仍称 not implemented | 实际已接实库并部署 |
| 根 `docs/产品调研与设计缺口` 多处仍称“大量 501” | 属历史研究快照，不应作为当前实施状态 |
| 看板 D5 仍可能显示 dossier 编辑器待开发 | admin 最新代码已经实现 |
| 总仓 submodule 指针同步 | 本次已更新为各子仓最终提交；另一台电脑须在 `git pull` 后执行 `git submodule update --init --recursive` |
| 子模块默认 detached HEAD | 只读快照属正常；继续开发前进入目标子仓执行 `git switch main`，README 已补完整流程 |
| 任务号 X3 曾冲突 | 已统一：X3 为双机对聊房间，可靠性修复改为 X6 |
| 看板 X3 编号曾在不同版本中指代不同任务 | 看板负责人提交前需统一编号，避免“双机对聊”和“可靠性修复”冲突 |

## 7. 未完成

### 7.1 用户端

- “我的星仔”角色档案页尚未实现；字段可见/可编辑边界仍待产品拍板。
- `persona_growth` 成长建议卡和应用建议二次确认尚未实现。
- 人设“已验证生效”第四态尚未实现。
- 配网引导缺最终固件步骤和图文素材。
- 设备改名/解绑、成长建议、每日运势卡、八字录入已随新构建上线，待真实账号验收。
- 桌面端宽屏分栏和窗口状态记忆尚未实现。
- 数据导出 A8、记忆画像 A9、人设问卷 A10 与“我的星仔”档案已在 App 上线，待真实数据验收。

### 7.2 管理台

- 分析结果仍以原始 JSON `<pre>` 为主，尚未卡片化展示摘要、主题、情绪和成长建议。
- 设备资产列表仍以 `limit=100` 一次拉取，没有真实分页控件。
- 页面空态、加载和错误提示尚未统一。
- admin 仓内 docs/02、03、04、06 的事实回写仍有滞后。

### 7.3 后端与内容运营

- E10/E10.3 每日运势、八字、定时生成、东八区切日与两步联网检索已部署；待 App 重新部署后做真实账号验收。
- 趣味测试、作答回看、可选写入记忆、简略星盘与 `share_card` 已部署；ruff/mypy 通过，pytest 112 项通过。
- E6.1 `memory_profile`、E2.1 问卷/后端 MBTI 算型/persona preview 已部署。
- E7.1 KB 反馈 accept 只建 draft 的闭环已部署。
- E8 同步 JSON export、90/180 天数据保留清理和 `/admin/ops/metrics` 已部署。
- KB v3 的 12 星座 + 16 MBTI 第一人称宠物视角内容已按 version++ 发布。
- `daily_summary` 生成时机、失败可见性、重试策略和保留期限尚待产品定稿。

### 7.4 固件和硬件

- OTA 资产迁移尚未完成，是第二只眼的前置阻塞。
- 第二只眼硬件和软件尚未完成。
- PetBehaviorController、WS2812、双舵机尚未完成。
- K230 UART 视觉尚未完成。
- 端侧离线人格、主动 idle 行为和触摸交互尚未完成。
- ESP32-S3 硬件方向尚未正式拍板；现有审核结论为可做 EVT，但当前不满足正式投板 Go。

### 7.5 运维与发布

- ai-pet-ops 采集器尚未正式部署到服务器。
- 状态变化告警通道尚未配置。
- 域名、ICP、Caddy 443、WSS/MQTTS 正式入口尚未完成。
- 备份年龄、证书到期、部署版本等运维指标尚未形成正式展示或告警闭环。

## 8. 未开始

以下事项目前只有设计、排期或产品讨论，没有可用实现：

- 双机对聊房间实时桥、轮流锁和配对入口。
- backend E9 社交端点和 BLE 好友体系。
- Feed、排行榜和重型养成系统。
- App 数据再导入/换机恢复闭环。
- 主动记忆召回触发器和主动话题下行通道。
- 宠物人格状态机、饥饿/心情/精力/冷落状态。
- 北极星指标正式埋点和运营统计端点。
- 共同经历 `shared_moments` 领域模型。
- S3 专用 `esp32-s3-ai-pet` 固件板型。
- 4G 和 K230 在正式硬件上的完整联调。
- 正式生产域名和公网发布。

## 9. 分仓状态速查

| 仓库/目录 | 已完成/已验收重点 | 当前主要缺口 | 综合状态 |
|---|---|---|---|
| `ai-pet-backend` | 核心业务、E10/E10.3、E6.1-E8、KB v3、趣味测试/星盘已上线 | E11 冻结 | 可内测，继续验收 |
| `xiaozhi-server` | 实时语音、旁路、C5、思考隔离、Memory MCP、b12 可靠性修复 | X1 剩余真机证据 | 主链可用，待收口 |
| `ai-pet-admin` | 运营功能覆盖较完整，dossier 已实现 | 卡片化、分页、状态规范、文档债 | 可运营验收 |
| `ai-pet-app` | 主链、档案、A8/A9/A10、E10、趣味测试/星盘已上线 | 配网、A3 状态字段、正式 HTTPS 发布；新功能待真实账号验收 | 可 HTTP 内测 |
| `ai-pet-ops` | 只读采集骨架完成 | 未部署、无告警 | 骨架阶段 |
| `prototype` | 总览、验收墙、人设和档案原型完成 | 真机后更新、admin 分析视觉稿 | 产品评审可用 |
| `xiaozhi-esp32` | P4 单眼和眼睛 MCP 已工作 | OTA 空间、资产复现、第二眼、误唤醒、行为层 | 真机原型阶段 |
| `hardware` | S3 原理图完成软件视角审核 | 三个投板 P0、S3 是否进 V0.3 | 当前 NO-GO |
| `work_dashboard` | 跨仓状态体系已建立 | 本地/远端和任务号冲突 | 需先同步协调 |

## 10. 子项目开工、阻塞与拍板清单

### 10.1 可以立即开工或继续

| 优先级 | 子项目/任务 | 开工条件与边界 |
|---|---|---|
| P0 | xiaozhi-server X1 剩余真机验收 | b12 已部署，可立即采集五类旁路、S1-S5、BIZ 日志和一次真实 `memory.search` 证据 |
| P1 | backend 趣味测试/简略星盘 | ✅ `4b2fc62` 已部署；迁移 0010，112 测试通过，转真实账号验收 |
| P0 | App A2/A7/A13/A14 上线验收 | 代码已推送；重新部署新构建并用真实账号验收，不再重复开发 |
| P1 | Admin D1-D5 | ✅ 设备列表/详情、分页、统一状态与分析卡已部署；继续真实运营数据验收 |
| P1 | 固件 F1 | OTA 资产迁移及干净克隆眼睛资产可复现性可立即处理，是第二只眼前置 |
| P1 | Ops 监控/告警实现 | 本地实现可开工；正式上生产服务器前需要部署授权 |
| P1 | App A5 真实数据验收 | 只读验收可立即开展，不与 A7 代码开发冲突 |
| P2 | Prototype R3 | Admin 分析卡视觉稿可独立开工 |
| P1 | App A1/A8/A9/A10 + 测试/星盘 | ✅ `main=35f297c`（功能 `ffd04ae`）已部署；转真实账号/数据验收 |

### 10.2 当前阻塞

| 任务 | 阻塞原因 | 解锁条件 |
|---|---|---|
| App A4 配网引导 | 缺固件最终配网步骤和图文素材 | 固件侧交付可复现步骤与素材 |
| App A8 数据导出 | 无阻塞，已部署 | 用真实账号验证 JSON 下载内容 |
| App A9 记忆画像 | 无阻塞，已部署 | 用真实会话数据验证画像卡 |
| App A10 人设问卷 | 无阻塞，已部署 | 用真实账号验证问卷与后端算型 |
| App A11 PWA 正式发布 | 无正式域名、ICP、HTTPS | O1 完成 |
| App A13 每日运势卡片 | backend 与 App 均已部署 | 做真实账号生成链路验收 |
| App A14 八字录入 | backend 与前端代码已完成；隐私边界和 HTTPS 未完成 | 内测验收可做；对外采集前须拍板隐私并完成 HTTPS |
| 固件第二只眼 | OTA 容量紧张且 F1 未完成 | 先完成 OTA 资产迁移和空间评估 |
| Prototype R4 | 缺完整真机验收结果 | X1 完成 |
| 正式发布 | 真机、域名/证书、监控均未收口 | 完成 X1、O1、O2 |

### 10.3 需要产品负责人拍板

| 决策 | 建议默认方案 | 不拍板的影响 |
|---|---|---|
| dossier 可见/可编辑边界 | 六字段均可见；关系/陪伴偏好可编辑，其余只读；Admin 可全量编辑 | 阻塞 App “我的星仔”档案实现 |
| 八字隐私边界 | Admin 不见原始数据；App 只显示已配置和脱敏摘要；用户可覆盖/删除；原始数据不进入 persona、C5、日志；HTTPS 前不采集 | 阻塞 E10.2、A14 和数据合规设计 |
| E10 联网检索源 | 已定 MiniMax M3 检索 + 现网模型分发两步链路 | ✅ 已实现并部署；当日 12 星座已生成联网摘要 |
| S3 硬件路线 | 不替换 P4 V0.2；下载路径、功放供电、4G UART 电平三个 P0 关闭后仅并行做 EVT | 阻塞投板与 V0.3 BOM/固件排期 |
| WakeNet 调优目标 | 优先降低误唤醒，接受远场灵敏度略降 | 阻塞固件阈值和麦增益最终参数 |
| 闭眼后断连语义 | 固件不要“一闭眼就断”；由服务端区分普通闭眼与睡眠/休息意图 | 阻塞 S2 行为定稿 |
| 每日摘要与保留策略 | 会话结束入队；按设备/日期幂等；原始消息 90 天、摘要 180 天 | E8 清理已部署；失败重试与运营体验继续真实数据验收 |
| 双机对聊首版范围 | X1 前不抢占；首版控制台配对、仅内存、不持久化、最多 3 轮 | 阻塞后续 X3 编号和实现边界 |
| 正式域名/ICP备案主体 | 尽快确定域名和备案主体 | 直接阻塞 HTTPS、PWA 和正式外部发布 |

### 10.4 技术协调事项（不需要产品拍板）

- `work_dashboard` 已推送 `3451daf`；本次总仓提交记录其与全部子仓最终指针。
- backend、Admin 和 App 均有专属并行开发改动；新会话只能做不重叠的只读验收或其他模块。
- 天气 API Key 疑似进入版本库，需由持有云端权限的人轮换，并按密钥泄露流程清理；不得把新密钥写入文档或 Git。
- 固件眼睛 `.bin` 生成资产在干净克隆不可复现，应通过构建生成或确定性资产入库解决。

## 11. 建议执行顺序

1. 完成 X1 真机验收和 App 新功能真实账号/数据验收。
2. 立即执行 X1 剩余真机验收，保存 BIZ 日志、backend 落库和设备表现三侧证据。
3. 轮换并移除仓库中的天气 API Key。
4. 本次已提交推送看板与固件设计稿；总仓提交最终 submodule 指针后形成可跨设备复现快照。
5. 修复固件眼睛资产生成/提交机制，完成 OTA 资产迁移。
6. 可并行启动 Ops 本地实现和 Prototype R3；App 转验收与缺陷修复。
7. E6.1/E2.1/E7.1/E8 与趣味测试/简略星盘已完成；E11 等架构拍板。
8. 尽快拍板域名/备案、隐私和硬件路线；完成 HTTPS/WSS 与 ai-pet-ops 正式部署。
9. 关闭 S3 三个硬件 P0 后再决定 EVT 投板和 V0.3 硬件路线。
## 12. 维护规则

- 本文只记录状态分类，不复制接口 schema；接口仍以 backend `docs/06` 和 xiaozhi `docs/05` 为准。
- “代码已完成”不得直接改成“已验收”，必须附构建、部署、浏览器、容器或真机证据。
- 新增 Bug 时注明影响范围、是否已有修复、修复是否部署、是否完成回归。
- 每次阶段里程碑后同步更新正式协作看板；本文可按日期生成新快照，不覆盖历史事实。
- 不在本文记录密码、Token、私钥、公钥全文或生产 `.env` 内容。
