# 开发完成与归档清单（2026-09-20）

对照 [[08-协作反馈日志]]、[[09-下一步开发方案]]、固件 `AI_PET_PROGRESS_zh.md`、`BOARD.md`，以及看板联调状态。  
**明细以同目录 Excel 为准**：[11-开发完成与归档清单.xlsx](11-开发完成与归档清单.xlsx)（用 Excel / WPS 打开，按「归档建议」筛选）。

本文件只写结论。契约仍以 `ai-pet-backend/docs/06`、`xiaozhi-server/docs/05` 为准。密钥不写。

## 归档口径

| 建议 | 含义 |
|------|------|
| **可归档** | 开发完成，且有真机或部署证据。本阶段不必再做。 |
| **条件归档** | 主路径已过，还差一次回归或补记结论。不挡下一阶段设计。 |
| **不可归档** | 缺口仍在，必须继续做或先取证。 |
| **冻结** | 硬件未关或跨仓未拍板。禁止开工。 |

## 现在可以归档的

这些可以当成「本阶段做完了」，不必再开新需求：

1. **S3 独立板型到板**：配网、OTA、激活、WS、双眼点亮。
2. **P1 同轮眼+灯**：开心 / 难过 / 生气同一毫秒改脸和灯。COM28 用户确认。
3. **巨蟹座皮肤**：五表情真机通过。电脑侧三套预览 / 黑底 / RGB565 也可归档。
4. **休息断开**：闭眼 + S2「晚安」+ WS 断开。
5. **K230 UART1 JSON**：有 vision / tracking。
6. **Memory MCP 的 ILIKE 实现 + 容器级 search**：代码完成。不要在固件再做一份记忆。
7. **职责分层**：记忆在云，身体在端。`self.memory.*` 不注册。
8. **S7**：不播 think、C5 首轮上下文、承认星座/MBTI。
9. **跟随方案文档**（只归档方案，不归档实现）：Δx，禁止回中。
10. **hwtest 工程**：隔离硬件用，不替代量产体验。

拍照识图、语音主链、天蝎皮肤标 **条件归档**：主路径过了，豆包对话还没回归，天蝎缺一行结论。

## 不能归档、必须接着做

按这个顺序，不要跳：

1. 休息后再唤醒自动 `self.eye.open`（当前仍 `closed=1`）。
2. 新会话看 `default_emotion` 是否同轮改脸和灯。
3. 真机一句偏好 → 日志出现 `memory.search` → 同轮眼+灯。

这三件不过，**嵌入式和云端记忆只在职责上对齐，运行时没有闭环**，P2 不能开。

另外还没归档、但不要插队：

- `blink_profile`：契约三种形状（对象 / 字符串 / 「固件无工具」）和固件 `interval_ms` 未对齐。
- 双鱼未上板；外设旁路真机落库；S1/S3/S4 体验回归。

## 明确冻结（当已归档的「不做」）

4G、喇叭飞线前听感、舵机/跟随实现、E11、E9、正式 HTTPS、眼睛画聊天、固件本地记忆、S8 续聊、P3 向量、P4 协调器。

## 阶段判断

| 阶段 | 判断 |
|------|------|
| P0 到板 | **可收口**（舵机项记硬件未接） |
| P1 同轮联动 | **主项可归档**；醒后睁眼是唯一功能缺口 |
| 记忆 ILIKE | **实现可归档，真机不可归档** |
| P2–P4 | **全部不可归档** |

## Excel 怎么看

打开 `11-开发完成与归档清单.xlsx`：

- **说明**：口径和当前结论。
- **总览**：条数 + 饼图。
- **全量清单**：筛选「归档建议」或「阶段」。
- **可归档 / 条件归档 / 不可归档 / 冻结**：拆开的表。

看板仓若仍脏，本清单只落在本目录，不混提交看板。
