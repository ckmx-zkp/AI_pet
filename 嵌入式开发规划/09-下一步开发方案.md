# 下一步开发方案（2026-09-20）

依据 [[08-协作反馈日志]]。**P1 同轮眼+灯已于 2026-09-20 COM28 真机通过**（用户确认 + 串口同毫秒 DualEye/LedMood）。不要改跟随，不要开 P2 契约。

## 硬件端做到哪了

分三层，不要把「代码写了」当成「板上过了」。

### 真机已经过的（更早会话）

| 项 | 证据 |
|----|------|
| 大板能配网、语音、OTA | 主链历史；口常用 CH340K，COM25 / COM27/28，不是 COM14 |
| 双眼点亮、C1 表情 | 128×160，GC9107 |
| U4 拍照 + 云端识图 | COM25；对话 LLM 须 M2.5，视觉 M3 |
| K230 UART1 JSON | 有 vision / tracking 字段 |
| 4G | **明确失败**：USB 5V 到不了模组 9V，AT 0 字节 |

### 代码已写、真机已过（2026-09-20 COM28）

| 项 | 仓库 | 真机 |
|----|------|------|
| `set_emotion` 同轮改 WS2812 | `eye_controller.h` 已接 `ApplyEmotion` | **已过**：开心/难过/生气同轮 |
| `self.eye.close` + 服务端 S2 | 既有工具 | 日志：`closed=1`、「晚安，先休息啦。」、WS 断开 |
| `self.eye.set_blink_profile` | 只吃 `interval_ms` | 工具已注册，未单独话术验 |
| 听时视线轻漂、说时停漂、idle 45s 眼回中性 | `dual_eye_display` | 未单独立项 |
| 云端 `default_emotion` 映射 | 小智侧已有 | **重连未看到**；休息后再唤醒仍 `closed=1`/`angry` |

### 硬件没接，代码先停手

| 项 | 状态 |
|----|------|
| CN2 舵机、触摸 | **用户确认未接** |
| `FaceFollowController` | 仍是 `tracking.dx` **回中**；正确方案在 `AI_PET_FACE_FOLLOW_PLAN_zh.md`（Δx 位移），**未改代码、不要为跟随去烧** |
| 喇叭 | PA_EN=GPIO46，没飞线就当无声，不挡 P1 眼灯验收 |

### 阶段判断

- P0：眼 / 灯 / 拍照 / K230 **部分真机过**；舵机勾不了；喇叭未关。
- P1：**同轮眼+灯已过**；默认情重连、醒后自动睁眼未过。
- P2–P4、4G、E11、E9：不动。

---

## 下一步（P1 主项已过）

1. 休息后再唤醒：固件是否应在新会话自动 `self.eye.open`（当前仍 `closed=1`）。
2. 新会话观察 `default_emotion` 是否同轮改脸和灯。
3. 才允许 X1：真机一句偏好 → `memory.search` → 同轮 `self.eye.set_emotion`（仍无固件记忆工具）。
4. 舵机焊上之后：先改 Δx 位移跟随，再烧跟随包。没焊不准改 `face_follow_controller`。
5. 飞线 PA_EN 后再排听感。
6. P2 embodiment 字段要先改 `docs/06` / 小智 `docs/05`。

## 优先测试计划：星座眼睛素材（2026-09-20 补充）

### 已完成：三套 PNG 素材

天蝎座、双鱼座、巨蟹座各 **8 张，共 24 张**，已实际保存到固件仓 `image/zodiac/` 的对应目录；主眼文件均为 `eye_master.png`。后续情绪与眨眼图使用内置 image_gen，分别引用各自星座主眼生成。

资源根目录：`D:/Home_Work/ESP32_XIAOZHI/xiaozhi-esp32/image/zodiac/`。

| 星座 | 子目录 | 配色 |
|------|--------|------|
| 天蝎座 | `scorpio/` | 绯红、深紫 |
| 双鱼座 | `pisces/` | 梦幻蓝、淡紫 |
| 巨蟹座 | `cancer/` | 月光银蓝、珍珠青 |

每个目录的文件清单：

| 文件 | 用途 |
|------|------|
| `eye_master.png` | 普通 / 中性，也是本套参考主眼 |
| `eye_happy.png` | 喜：温和开心 |
| `eye_angry.png` | 怒：右眼内侧压低 |
| `eye_sad.png` | 哀：外侧下垂 |
| `eye_joy.png` | 乐：更明显的笑眼 |
| `eye_blink_30.png` | 眨眼第一帧 |
| `eye_blink_70.png` | 眨眼第二帧 |
| `eye_blink_closed.png` | 眨眼接近闭合；不是睡眠纯黑帧 |

- [三套图片集中预览](../ESP32_XIAOZHI/xiaozhi-esp32/image/zodiac/preview.html)
- [素材说明与限制](../ESP32_XIAOZHI/xiaozhi-esp32/image/zodiac/README.md)
- [文件检查记录](../ESP32_XIAOZHI/xiaozhi-esp32/image/zodiac/validation.json)：24 张均可解码、尺寸为 1254×1254，文件名齐全、哈希无重复。
- 各目录的 `prompt.txt`、`emotion_prompts.json` 保存生成提示词；天蝎和双鱼另有 `eye_angry_final_prompt.txt`，记录怒表情眼睑方向的最终修正。
- 完整可复制提示词已集中归档到 [[10-星座眼睛生成提示词]]，包括参考图选择、输出命名与最终修正版。

**状态（2026-09-20）**：电脑预览 1–3 已过。三套均已转 128×160 RGB565。固件当前嵌入并已烧 **COM28 = 巨蟹座**，用户确认五表情通过。天蝎烧过未单独归档结论。双鱼已转未嵌。无运行时换皮肤。`self.led.set_zodiac` 只改灯色。

### 测试顺序与通过标准

继续先收口上文的“休息后再唤醒”和“新会话默认表情”回归。星座素材作为 P1 视觉专项，按下列顺序推进，不以此跳过现有回归。

| 顺序 | 待测试项 | 通过标准 |
|------|----------|----------|
| 1 | 电脑预览三套五表情 | ✅ 天蝎绯红、双鱼蓝紫、巨蟹银青可辨；喜/乐、怒/哀可辨；无第二只眼或文字 |
| 2 | 黑底、边界与帧间对齐 | ✅ 主眼 RGBA 已压黑底；表情原已是黑底。128×160 预览虹膜居中、无白边 |
| 3 | 缩小与 RGB565 预览 | ✅ 各套 `s3_128x160/*_128x160.png`；瞳孔与眼睑仍清楚 |
| 4 | 单套试接与增量构建 | ✅ 天蝎、巨蟹均已嵌入并烧过 COM28。双鱼已转未嵌 |
| 5 | 真机五表情与眼灯联动 | ✅ 巨蟹用户确认通过；P1 开心/难过/生气同轮眼+灯此前已过。天蝎烧过未单独归档 |
| 6 | 眨眼、视线、休息与再唤醒 | 休息+S2 已过；再唤醒仍 `closed=1`。眨眼/看左未单独立项 |
| 7 | 三套结果归档 | 巨蟹✅；天蝎未记结论；双鱼未上板 |

转换已支持 `--src/--out/--preview/--install`。其余九个星座、运行时换皮肤尚未实现。

## 明确不要做

改跟随算法、为跟随而烧录、4G、`self.memory.*`、P2 契约、聊天画到眼睛、第二路舵机、对话 LLM 改 M3。
